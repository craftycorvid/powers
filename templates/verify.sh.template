#!/usr/bin/env bash
# scripts/verify.sh — the repo's verify contract. The powers verify-gate runs
# this when a subagent finishes; non-zero exit blocks the subagent. Delegate
# to whatever "the code is good" means here — keep it fast and deterministic.
set -euo pipefail
cd "$(dirname "$0")/.."

# Must be safe in a fresh checkout: isolation:worktree agents start with no
# deps installed. Uncomment (and adapt) the bootstrap for your ecosystem:
# [ -d node_modules ] || npm ci --silent

npm test --silent          # <- replace with this repo's real test/build command
